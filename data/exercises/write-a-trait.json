{"id":"write-a-trait","title":"A trait of your own","difficulty":"core","chapter":"type-traits","topics":["traits","templates","specialization","metaprogramming"],"standard":"c++20","check":"unit","stdin":"","cases":[],"timeLimitMs":0,"promptHtml":"<p>Three traits to write, each a primary template plus one or more partial\nspecializations. None needs more than four lines.</p>\n<ul>\n<li><code>is_pair_v&lt;T&gt;</code> — true for any <code>std::pair</code>, false for everything else.</li>\n<li><code>rank_of_v&lt;T&gt;</code> — how many array dimensions <code>T</code> has: <code>0</code> for <code>int</code>, <code>1</code> for\n<code>int[3]</code>, <code>2</code> for <code>int[3][4]</code>. (<code>std::rank</code> already does this; write your own.)</li>\n<li><code>size_of_container_t&lt;T&gt;</code> — the type a container’s <code>size()</code> returns, or\n<code>std::size_t</code> if <code>T</code> has no <code>size()</code> at all.</li>\n</ul>\n<p>The starter has the primary templates only, so every answer is currently the\ndefault.</p>\n","starter":"#include <cstddef>\n#include <string>\n#include <type_traits>\n#include <utility>\n#include <vector>\n\ntemplate <class T>\nstruct is_pair : std::false_type {};\n\ntemplate <class T>\ninline constexpr bool is_pair_v = is_pair<T>::value;\n\ntemplate <class T>\nstruct rank_of : std::integral_constant<std::size_t, 0> {};\n\ntemplate <class T>\ninline constexpr std::size_t rank_of_v = rank_of<T>::value;\n\ntemplate <class T, class = void>\nstruct size_of_container { using type = std::size_t; };\n\ntemplate <class T>\nusing size_of_container_t = typename size_of_container<T>::type;","tests":"// is_pair\nstatic_assert(!is_pair_v<int>);\nstatic_assert(is_pair_v<std::pair<int, int>>);\nstatic_assert(is_pair_v<std::pair<std::string, std::vector<double>>>);\nstatic_assert(!is_pair_v<std::vector<int>>);\nCHECK(is_pair_v<std::pair<char, char>>);\nCHECK(!is_pair_v<std::string>);\n\n// rank_of\nstatic_assert(rank_of_v<int> == 0);\nstatic_assert(rank_of_v<std::string> == 0);\nstatic_assert(rank_of_v<int[3]> == 1);\nstatic_assert(rank_of_v<double[3][4]> == 2);\nstatic_assert(rank_of_v<char[2][2][2]> == 3);\nstatic_assert(rank_of_v<int[]> == 1);\nCHECK_EQ(rank_of_v<float[7][7]>, std::size_t{2});\n\n// size_of_container\nstatic_assert(std::is_same_v<size_of_container_t<std::vector<int>>,\n                             std::vector<int>::size_type>);\nstatic_assert(std::is_same_v<size_of_container_t<std::string>, std::string::size_type>);\nstatic_assert(std::is_same_v<size_of_container_t<int>, std::size_t>);\nstatic_assert(std::is_same_v<size_of_container_t<double>, std::size_t>);\nCHECK(std::is_same_v<size_of_container_t<std::vector<char>>, std::size_t>);","hints":["<p><code>is_pair</code> needs one partial specialization, <code>struct is_pair&lt;std::pair&lt;A, B&gt;&gt;</code>, with <code>A</code> and <code>B</code> deduced from the match.</p>\n","<p><code>rank_of</code> needs <strong>two</strong> specializations: one for <code>T[N]</code> (a template parameter of type <code>std::size_t</code> alongside the type) and one for <code>T[]</code>, the unknown-bound case. Both recurse into <code>rank_of&lt;T&gt;::value + 1</code>.</p>\n","<p>A non-type template parameter is declared like a function parameter: <code>template &lt;class T, std::size_t N&gt; struct rank_of&lt;T[N]&gt; : …</code>.</p>\n","<p><code>size_of_container</code> is the <code>void_t</code> idiom. The specialization’s second argument is <code>std::void_t&lt;decltype(std::declval&lt;const T&amp;&gt;().size())&gt;</code>, which is only a valid type when the expression compiles.</p>\n","<p>Inside that specialization, the answer is <code>decltype(std::declval&lt;const T&amp;&gt;().size())</code> — the same expression, this time used for its type.</p>\n","<p><code>std::declval</code> lives in <code>&lt;utility&gt;</code> and only works in unevaluated contexts like <code>decltype</code>. That is exactly where you need it.</p>\n"],"solution":"#include <cstddef>\n#include <string>\n#include <type_traits>\n#include <utility>\n#include <vector>\n\ntemplate <class T>\nstruct is_pair : std::false_type {};\n\ntemplate <class A, class B>\nstruct is_pair<std::pair<A, B>> : std::true_type {};\n\ntemplate <class T>\ninline constexpr bool is_pair_v = is_pair<T>::value;\n\ntemplate <class T>\nstruct rank_of : std::integral_constant<std::size_t, 0> {};\n\ntemplate <class T, std::size_t N>\nstruct rank_of<T[N]> : std::integral_constant<std::size_t, rank_of<T>::value + 1> {};\n\ntemplate <class T>\nstruct rank_of<T[]> : std::integral_constant<std::size_t, rank_of<T>::value + 1> {};\n\ntemplate <class T>\ninline constexpr std::size_t rank_of_v = rank_of<T>::value;\n\ntemplate <class T, class = void>\nstruct size_of_container { using type = std::size_t; };\n\ntemplate <class T>\nstruct size_of_container<T, std::void_t<decltype(std::declval<const T&>().size())>> {\n    using type = decltype(std::declval<const T&>().size());\n};\n\ntemplate <class T>\nusing size_of_container_t = typename size_of_container<T>::type;","solutionNotesHtml":"<p>Three specializations, three different things being matched.</p>\n<p><code>is_pair</code> matches a <strong>class template’s arguments</strong>. <code>rank_of</code> matches an\n<strong>array’s shape</strong>, including a non-type parameter for the bound, and recurses —\n<code>double[3][4]</code> is an array of 3 things of type <code>double[4]</code>, so peeling one\ndimension leaves a smaller array, and the primary template catches the base\ncase. <code>size_of_container</code> matches on <strong>whether an expression is well-formed</strong>,\nwhich is a different question entirely and needs the <code>void_t</code> machinery to\nexpress.</p>\n<p>The last check is worth reading twice: <code>size_of_container_t&lt;std::vector&lt;char&gt;&gt;</code>\nis the same type as <code>std::size_t</code> on this implementation, and the assertion\npasses for that reason — not because the default was used. Both branches\nhappen to agree here, which is a good reminder that a trait test that only\ncompares against the default proves nothing. The <code>static_assert</code>s above it\ncompare against <code>std::vector&lt;int&gt;::size_type</code> instead, which is what actually\ndistinguishes the two answers.</p>\n<p>In modern code you would write the third one as a concept and a\n<code>std::conditional_t</code>, and you would be right to. The <code>void_t</code> version is here\nbecause you will read it long before you get to delete it.</p>\n"}