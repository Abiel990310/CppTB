{"id":"erase-while-iterating","title":"Erase while iterating","difficulty":"core","chapter":"iterators","topics":["iterators","containers","lifetime"],"standard":"c++20","check":"unit","stdin":"","cases":[],"timeLimitMs":0,"promptHtml":"<p><code>drop_negatives</code> walks a vector and erases every negative element. It advances\nthe iterator on every pass <em>and</em> erases, so it invalidates the iterator it is\nholding and skips elements besides.</p>\n<p>Fix the loop. Do not use <code>std::erase_if</code> — the point is to get the manual\nversion right, since you will meet it in code you did not write.</p>\n","starter":"#include <vector>\n\nvoid drop_negatives(std::vector<int>& v) {\n    for (auto it = v.begin(); it != v.end(); ++it) {\n        if (*it < 0) {\n            v.erase(it);\n        }\n    }\n}","tests":"{\n    std::vector<int> v{1, -2, 3, -4, 5};\n    drop_negatives(v);\n    CHECK_EQ(v.size(), std::size_t{3});\n    CHECK_EQ(v[0], 1);\n    CHECK_EQ(v[1], 3);\n    CHECK_EQ(v[2], 5);\n}\n{\n    // Consecutive negatives: the broken version skips the second of each pair.\n    std::vector<int> v{-1, -2, -3, 4};\n    drop_negatives(v);\n    CHECK_EQ(v.size(), std::size_t{1});\n    CHECK_EQ(v[0], 4);\n}\n{\n    std::vector<int> v{-1, -2, -3};\n    drop_negatives(v);\n    CHECK(v.empty());\n}\n{\n    std::vector<int> v{1, 2, 3};\n    drop_negatives(v);\n    CHECK_EQ(v.size(), std::size_t{3});\n}\n{\n    std::vector<int> v;\n    drop_negatives(v);\n    CHECK(v.empty());\n}\n{\n    std::vector<int> v{5, -1};\n    drop_negatives(v);\n    CHECK_EQ(v.size(), std::size_t{1});\n    CHECK_EQ(v[0], 5);\n}","hints":["<p><code>erase</code> invalidates the iterator you passed it — but it <em>returns</em> an iterator to the element that followed.</p>\n","<p>Take the loop’s <code>++it</code> out of the header entirely.</p>\n","<p>Advance in exactly one branch: <code>it = v.erase(it)</code> when you removed something, <code>++it</code> when you did not.</p>\n","<p>The consecutive-negatives case is what catches the version that erases and then also increments.</p>\n"],"solution":"#include <vector>\n\nvoid drop_negatives(std::vector<int>& v) {\n    for (auto it = v.begin(); it != v.end(); ) {\n        if (*it < 0) {\n            it = v.erase(it);\n        } else {\n            ++it;\n        }\n    }\n}","solutionNotesHtml":"<p>The empty loop header — <code>for (init; condition; )</code> with nothing after the second\nsemicolon — is the signature of this pattern. If you see a loop that erases and\nstill increments in the header, it is wrong.</p>\n<p>The <code>{-1, -2, -3, 4}</code> case is the one that separates a real fix from a lucky\none. A version that erases and then increments skips the element that shifted\ninto the erased slot, so it leaves <code>-2</code> behind; with the trailing <code>4</code> there is\nstill an element to reach <code>end()</code> on, so it does not crash — it just returns the\nwrong answer. That is the failure mode to fear.</p>\n<p>In real code, prefer <code>std::erase_if(v, [](int x) { return x &lt; 0; })</code>. It is one\nline, cannot be written wrongly, and for a vector it is also faster: the manual\nloop moves the tail down on every erase, making it O(n²), while <code>erase_if</code> does\na single pass.</p>\n"}