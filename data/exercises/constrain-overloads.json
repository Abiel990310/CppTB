{"id":"constrain-overloads","title":"Let the constraint choose","difficulty":"stretch","chapter":"concepts","topics":["concepts","overloading","templates"],"standard":"c++20","check":"unit","stdin":"","cases":[],"timeLimitMs":0,"promptHtml":"<p><code>render</code> should produce a different string depending on what it is given:</p>\n<ul>\n<li>an integral type → <code>&quot;int:&lt;value&gt;&quot;</code></li>\n<li>a floating-point type → <code>&quot;float:&lt;value&gt;&quot;</code> with one decimal place</li>\n<li>anything with a <code>.size()</code> member → <code>&quot;sized:&lt;size&gt;&quot;</code></li>\n<li>anything else → <code>&quot;other&quot;</code></li>\n</ul>\n<p>Write four overloads, constrained so the compiler selects the right one. Do not\nuse <code>if constexpr</code> or tag dispatch — the selection must come from the\nconstraints.</p>\n","starter":"#include <concepts>\n#include <string>\n\n// Write the overloads here.","tests":"CHECK_EQ(render(42), std::string(\"int:42\"));\nCHECK_EQ(render(-7), std::string(\"int:-7\"));\nCHECK_EQ(render('a'), std::string(\"int:97\"));\n\nCHECK_EQ(render(2.5), std::string(\"float:2.5\"));\nCHECK_EQ(render(1.0f), std::string(\"float:1.0\"));\n\nCHECK_EQ(render(std::string(\"hello\")), std::string(\"sized:5\"));\nCHECK_EQ(render(std::vector<int>{1, 2, 3}), std::string(\"sized:3\"));\nCHECK_EQ(render(std::vector<int>{}), std::string(\"sized:0\"));\n\nstruct Opaque { int id; };\nCHECK_EQ(render(Opaque{1}), std::string(\"other\"));\nCHECK_EQ(render(nullptr), std::string(\"other\"));","hints":["<p>Write a <code>HasSize</code> concept with a requires-expression: <code>{ t.size() } -&gt; std::convertible_to&lt;std::size_t&gt;;</code></p>\n","<p><code>std::integral</code> and <code>std::floating_point</code> come from <code>&lt;concepts&gt;</code>.</p>\n","<p>Give <strong>every</strong> overload the same parameter form — <code>const T&amp;</code> throughout — or they tie on conversion before constraints are considered and the call is ambiguous.</p>\n","<p>For the one-decimal float, <code>std::format(&quot;{:.1f}&quot;, value)</code> is simplest; <code>&lt;format&gt;</code> provides it.</p>\n","<p><code>bool</code> and <code>char</code> are integral, which is why <code>render('a')</code> expects <code>&quot;int:97&quot;</code>.</p>\n"],"solution":"#include <concepts>\n#include <format>\n#include <string>\n#include <vector>\n\ntemplate <class T>\nconcept HasSize = requires(const T& t) {\n    { t.size() } -> std::convertible_to<std::size_t>;\n};\n\ntemplate <class T>\nstd::string render(const T&) { return \"other\"; }\n\ntemplate <HasSize T>\nstd::string render(const T& value) {\n    return \"sized:\" + std::to_string(value.size());\n}\n\ntemplate <std::integral T>\nstd::string render(const T& value) {\n    return \"int:\" + std::to_string(static_cast<long long>(value));\n}\n\ntemplate <std::floating_point T>\nstd::string render(const T& value) {\n    return std::format(\"float:{:.1f}\", value);\n}","solutionNotesHtml":"<p>The identical <code>const T&amp;</code> parameter across all four overloads is not a style\nchoice, it is a requirement. Overload resolution compares conversion sequences\nfirst and only consults constraints to break a tie among equally good\ncandidates. Give one overload a by-value parameter and the call becomes\nambiguous — with an error that says <code>call of overloaded 'render(int)' is ambiguous</code> and never mentions concepts, which is a genuinely confusing half hour.</p>\n<p>Note that <code>std::integral</code> and <code>HasSize</code> are unrelated concepts, and nothing\nsubsumes anything here. That is fine because no type satisfies both: an <code>int</code>\nhas no <code>.size()</code>, and a <code>std::vector</code> is not integral. Had a type satisfied\nboth, the call would be ambiguous and the fix would be to build one concept from\nthe other so subsumption has something to compare.</p>\n<p><code>render('a')</code> giving <code>&quot;int:97&quot;</code> is the <code>bool</code>-matches-<code>int</code> trap from Chapter 1.5\nin another guise: <code>char</code> is an integral type, so it selects the integral\noverload and <code>std::to_string</code> sees its numeric value. If you wanted characters\nhandled separately, that would be a fifth, more constrained overload —\n<code>std::same_as&lt;char&gt;</code>.</p>\n"}