{"id":"erase-evens","title":"Erase without invalidating","difficulty":"core","chapter":"algorithms","topics":["containers","algorithms","iterators"],"standard":"c++20","check":"unit","stdin":"","cases":[],"timeLimitMs":0,"promptHtml":"<p>Write <code>erase_evens</code>, which removes every even number from a <code>std::vector&lt;int&gt;</code>\nwhile keeping the odd ones in their original order.</p>\n<p>The obvious loop — iterate and call <code>erase</code> — invalidates the iterator you are\nholding. Use an approach that does not.</p>\n","starter":"#include <vector>\n\nvoid erase_evens(std::vector<int>& v) {\n}","tests":"std::vector<int> v{1, 2, 3, 4, 5, 6};\nerase_evens(v);\nCHECK_EQ(v.size(), std::size_t{3});\nCHECK_EQ(v[0], 1);\nCHECK_EQ(v[1], 3);\nCHECK_EQ(v[2], 5);\n\nstd::vector<int> all_even{2, 4, 6};\nerase_evens(all_even);\nCHECK(all_even.empty());\n\nstd::vector<int> none{1, 3};\nerase_evens(none);\nCHECK_EQ(none.size(), std::size_t{2});\n\nstd::vector<int> empty;\nerase_evens(empty);\nCHECK(empty.empty());","hints":["<p><code>std::erase_if(v, predicate)</code> does the whole job in C++20. Try it first, then try it the hard way.</p>\n","<p>The classic version is the erase-remove idiom: <code>v.erase(std::remove_if(v.begin(), v.end(), pred), v.end());</code></p>\n","<p><code>std::remove_if</code> does not remove anything. It shuffles the survivors to the front and returns where the new end is.</p>\n"],"solution":"#include <vector>\n#include <algorithm>\n\nvoid erase_evens(std::vector<int>& v) {\n    std::erase_if(v, [](int n) { return n % 2 == 0; });\n}","solutionNotesHtml":"<p><code>std::erase_if</code> arrived in C++20 precisely because the erase-remove idiom was\neasy to get half-right. The half that people forgot was the second argument to\n<code>erase</code>: without it you delete a single element instead of the whole tail, and\nthe vector quietly keeps stale values.</p>\n"}